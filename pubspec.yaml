name: lat_responsi_123230064
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  get:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  http: ^0.13.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^9.5.0

flutter:
  uses-material-design: true
